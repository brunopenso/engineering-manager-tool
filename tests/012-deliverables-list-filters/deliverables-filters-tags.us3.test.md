# US3: System tags portfolio filter

**Automated coverage**: `packages/web/tests/deliverables-portfolio-filters/`

## Scenarios

- Single tag filters to deliverables containing that tag
- Multiple tags use OR (at least one match)
- Empty tag selection does not exclude rows
- Tag options match catalog from `GET /tags/catalog`
