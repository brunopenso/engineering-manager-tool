# US3 Locale persistence — automated acceptance

Verify server-side `languagePreference` is saved via PATCH and restored on session bootstrap.

## Automated tests

- `packages/web/tests/web-i18n/profile-locale-persistence.us3.test.tsx`
- `packages/web/tests/web-i18n/login-detector.us3.test.ts`
# US3 Locale persistence — automated acceptance

Verify server-side `languagePreference` is saved via PATCH and restored on session bootstrap.

## Automated tests

- `packages/web/tests/web-i18n/profile-locale-persistence.us3.test.tsx`
- `packages/web/tests/web-i18n/login-detector.us3.test.ts`
