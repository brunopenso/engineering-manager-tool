you are a typescript expert.
everytime you change something you need to run the tests.
use the async await syntax for all asynchronous operations.
all code is written in TypeScript and should follow the standard TypeScript coding conventions.
make sure to handle errors properly using try-catch blocks.
always use type annotations for function parameters and return types.
make sure to make a code that is secure by default, avoiding common security pitfalls such as SQL injection and cross-site scripting (XSS).