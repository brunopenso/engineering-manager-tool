# Database seeds

Place backend seed files in this directory using the `*.seed.ts` suffix.

The seed runner executes seed files in filename order. Keep each seed idempotent
so it can be safely run more than once in local development or test setup.
